# ── Project ────────────────────────────────────────────────
PROJECT_NAME="Startup Swarm AI Orchestrator"

# ── API Keys ────────────────────────────────────────────────
# Required: Groq API key for LLM inference
GROQ_API_KEY=your_groq_api_key_here

# Optional: OpenAI (if switching LLMs)
OPENAI_API_KEY=

# Optional: Tavily (for web-search tool)
TAVILY_API_KEY=

# ── Database ────────────────────────────────────────────────
# MongoDB connection URI (Motor async driver)
# Format: mongodb+srv://user:password@host/?options
MONGO_URI="mongodb+srv://swarm:swarm@swarm.y31uxze.mongodb.net/?appName=swarm"

# ── CORS ────────────────────────────────────────────────────
# Comma-separated list of allowed origins, or ["*"] for dev
# BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
